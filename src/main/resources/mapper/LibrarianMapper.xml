<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.repository.LibrarianRepository">
    <select id="check" resultType="java.lang.String">
        SELECT pwd FROM librarian WHERE account = #{account}
    </select>
</mapper>